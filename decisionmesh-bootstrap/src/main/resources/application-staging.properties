# ==========================
# OIDC - STAGING profile
# ==========================
quarkus.oidc.enabled=true
quarkus.oidc.auth-server-url=https://keycloak.staging.example.com/realms/my-realm-staging
quarkus.oidc.client-id=my-app-backend
quarkus.oidc.credentials.secret=${OIDC_CLIENT_SECRET}

quarkus.oidc.token.issuer=https://keycloak.staging.example.com/realms/my-realm-staging
quarkus.oidc.token.audience=my-app-backend
quarkus.oidc.application-type=service

quarkus.oidc.tls.verification=certificate-validation

# CORS for staging React UI
quarkus.http.cors=true
quarkus.http.cors.origins=https://app.staging.example.com
quarkus.http.cors.methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
quarkus.http.cors.headers=Accept,Authorization,Content-Type,X-Requested-With
quarkus.http.cors.exposed-headers=Location,Info
quarkus.http.cors.access-control-max-age=24H
quarkus.http.cors.access-control-allow-credentials=true

quarkus.log.category."io.quarkus.oidc".level=INFO