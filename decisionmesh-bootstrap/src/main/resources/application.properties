# ==========================
# HTTP Security (shared across all profiles)
# ==========================

# 1 - OPTIONS preflight must be evaluated first
quarkus.http.auth.permission.preflight.paths=/*
quarkus.http.auth.permission.preflight.methods=OPTIONS
quarkus.http.auth.permission.preflight.policy=permit
quarkus.http.auth.permission.preflight.priority=1

# 2 - Public endpoints
quarkus.http.auth.permission.public.paths=/q/*,/health/*,/metrics
quarkus.http.auth.permission.public.policy=permit
quarkus.http.auth.permission.public.priority=2

# 3 - Secure API (evaluated last)
quarkus.http.auth.permission.api.paths=/api/*
quarkus.http.auth.permission.api.policy=authenticated
quarkus.http.auth.permission.api.priority=3

# Tells Quarkus which config property to read for the OIDC server URL
app.oidc.provider=keycloak
quarkus.oidc.auth-server-url=${app.oidc.issuer-url}